# 배포 폴더에 실수로 포함되지 않도록 하는 디자인 원본/참고/백업 자료
# (실제 페이지에서 참조되지 않는 파일만 포함 — 사용 중인 폴더는 절대 포함하지 않음.
#  삭제 없이 git 추적에서만 제외)
backup-before-tab-v2-assets/
메인샘플.png
메인히어로세션/
상단 이벤트 바/
카드슬라이드 이미지/
탭 세부 예상 디자인/

# Windows 리다이렉션 실수로 생성되는 파일
nul

# 민감 정보·빌드 산출물
.env
*.map
Thumbs.db
.DS_Store
